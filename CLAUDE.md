`apps`直下のフォルダは、それぞれ独立したアプリケーションとして動作します。
基本的に静的ファイルのみで構成、あるいはビルド後に静的ファイルとして配信されるアプリケーションが配置されています。
まずは、アプリケーション直下の`README.md`を見つけて読んでみましょう（apps/<project_name>/README.md）。

注意点：

- Typescriptからコンパイルされているか必ず確認して、Typescriptでコンパイルされている場合はjavascriptを直接編集しないでください。
  必ずTypescriptのソースコードを編集し、再コンパイルしてください。
- `config.yaml`がある場合は、シンプルなサーバーから配信されるアプリケーションです。
  （モジュール等を利用しているため、直接ブラウザで開くことはできない場合に活用します）
- CDNを活用する場合は、`cdn_resources`フォルダのスクリプトを優先的に利用しています。
